# Node / Expo
node_modules/
.expo/
.expo-shared/
dist/
web-build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# Environment
.env
.env.*
!.env.example

# Build artifacts
android/
ios/
build/
coverage/
.eas/

# Claude Code (contains EXPO_TOKEN and other secrets)
.claude/

# IDEs
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
Desktop.ini

# TypeScript
*.tsbuildinfo

# Jest
/coverage

# Logs
npm-debug.*
yarn-debug.*
yarn-error.*
*.log

# Legacy Swift scaffold (archived but kept out of git if desired)
# _legacy-swift/

# Portable Node install used when nvm-windows is unavailable.
# Created when bootstrapping on Windows 10 without admin rights.
# Never commit ~30 MB of Node binaries to git.
.local-node/

# Compiled output of the local Expo config plugin. Source of truth is
# `modules/expo-live-photo-exporter/plugin/src/`. The `build/` directory is
# regenerated by the `prepare` / `postinstall` script after every `npm install`.
modules/expo-live-photo-exporter/plugin/build/
